# *.tfvars
# *.tfvars.json

# .tfstate files
# These files are used to store the state of your infrastructure.
# They may contain sensitive information, so they should not be versioned.
*.tfstate
*.tfstate.backup

# .terraform directory
# This directory contains the provider plugins and modules.
# It is generated by `terraform init` and should not be versioned.
.terraform/

# Crash log files
crash.log

# Ignore override files as they are usually used for local development and contain
# sensitive data.
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Terraform plan files
*.tfplan

# CLI configuration files
.terraformrc
terraform.rc

# Terraform lock file
.terraform.lock.hcl

plans/